import PageBuilder from '../components/PageBuilder';

/**
 * About page
 * Composed entirely from config/sections.js under the "about" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function About() {
  return <PageBuilder page="about" />;
}
