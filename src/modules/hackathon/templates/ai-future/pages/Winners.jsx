import PageBuilder from '../components/PageBuilder';

/**
 * Winners page
 * Composed entirely from config/sections.js under the "winners" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Winners() {
  return <PageBuilder page="winners" />;
}
