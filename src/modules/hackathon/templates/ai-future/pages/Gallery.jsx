import PageBuilder from '../components/PageBuilder';

/**
 * Gallery page
 * Composed entirely from config/sections.js under the "gallery" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Gallery() {
  return <PageBuilder page="gallery" />;
}
