import PageBuilder from '../components/PageBuilder';

/**
 * Certificates page
 * Composed entirely from config/sections.js under the "certificates" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Certificates() {
  return <PageBuilder page="certificates" />;
}
