import PageBuilder from '../components/PageBuilder';

/**
 * LiveEvent page
 * Composed entirely from config/sections.js under the "live" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function LiveEvent() {
  return <PageBuilder page="live" />;
}
