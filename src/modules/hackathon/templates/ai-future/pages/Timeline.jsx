import PageBuilder from '../components/PageBuilder';

/**
 * Timeline page
 * Composed entirely from config/sections.js under the "timeline" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Timeline() {
  return <PageBuilder page="timeline" />;
}
