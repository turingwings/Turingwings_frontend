import PageBuilder from '../components/PageBuilder';

/**
 * Contact page
 * Composed entirely from config/sections.js under the "contact" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Contact() {
  return <PageBuilder page="contact" />;
}
