import PageBuilder from '../components/PageBuilder';

/**
 * Tracks page
 * Composed entirely from config/sections.js under the "tracks" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Tracks() {
  return <PageBuilder page="tracks" />;
}
