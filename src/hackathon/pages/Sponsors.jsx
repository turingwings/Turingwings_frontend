import PageBuilder from '../components/PageBuilder';

/**
 * Sponsors page
 * Composed entirely from config/sections.js under the "sponsors" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Sponsors() {
  return <PageBuilder page="sponsors" />;
}
