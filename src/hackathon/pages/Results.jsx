import PageBuilder from '../components/PageBuilder';

/**
 * Results page
 * Composed entirely from config/sections.js under the "results" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Results() {
  return <PageBuilder page="results" />;
}
