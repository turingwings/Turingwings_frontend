import PageBuilder from '../components/PageBuilder';

/**
 * Registration page
 * Composed entirely from config/sections.js under the "registration" key.
 * To change what appears on this page, edit that config — not this file.
 */
export default function Registration() {
  return <PageBuilder page="registration" />;
}
